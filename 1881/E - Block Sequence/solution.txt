package main import (	"bufio"	"fmt"	"math"	"os"	"strconv"	"strings") var (	reader = bufio.NewReader(os.Stdin)) func getInt() int {	s, _ := reader.ReadString('\n')	s = strings.TrimSpace(s)	n, _ := strconv.Atoi(s)	return n} func getIntList() []int {	s, _ := reader.ReadString('\n')	s = strings.TrimSpace(s)	parts := strings.Fields(s)	res := make([]int, len(parts))	for i, p := range parts {		res[i], _ = strconv.Atoi(p)	}	return res} func main() {	t := 1	t = getInt() 	for _ = range t {		solve()	}} func solve() {	n := getInt()	l := getIntList() 	// Memoization map: index -> result	memo := make(map[int]int) 	var dp func(int) int	dp = func(ind int) int {		if ind >= n {			return 0		} 		if val, ok := memo[ind]; ok {			return val		} 		include := math.MaxInt32		if ind+l[ind] < n {			include = dp(ind + l[ind] + 1)		} 		exclude := dp(ind+1) + 1 		res := min(include, exclude)		memo[ind] = res		return res	} 	fmt.Println(dp(0))} func min(a, b int) int {	if a < b {		return a	}	return b}