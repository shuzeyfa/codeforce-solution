import sys nums = [4, 8, 15, 16, 23, 42] # Query a1 * a2print("? 1 2", flush=True)p12 = int(input()) # Query a2 * a3print("? 2 3", flush=True)p23 = int(input()) # Find a2 safelya2 = Nonefor x in nums:    if p12 % x == 0 and p23 % x == 0:        a1 = p12 // x        a3 = p23 // x        if a1 in nums and a3 in nums and a1 != x and a3 != x:            a2 = x            break # Recover a1, a2, a3ans = [a1, a2, a3] # Query a3 * a4print("? 3 4", flush=True)p34 = int(input())a4 = p34 // a3ans.append(a4) # Query a4 * a5print("? 4 5", flush=True)p45 = int(input())a5 = p45 // a4ans.append(a5) # Find the remaining number (a6)for x in nums:    if x not in ans:        ans.append(x)        break # Output the answerprint("!", *ans, flush=True)